PORT=3001
JWT_SECRET=change-this-with-a-long-random-string
APP_USERNAME=hermann
APP_PASSWORD=change-this-password
VITE_API_BASE_URL=
# Per Android/Windows wrapper o frontend su dominio diverso:
# APP_CORS_ORIGINS=http://localhost,capacitor://localhost,https://tuo-dominio-app.com
# APP_CROSS_ORIGIN_AUTH=true
# Optional: use a pre-generated hash instead of APP_PASSWORD
# APP_PASSWORD_HASH=$2b$12$replace.with.bcrypt.hash
# Demo storage file
DB_PATH=/tmp/apphermann.json
